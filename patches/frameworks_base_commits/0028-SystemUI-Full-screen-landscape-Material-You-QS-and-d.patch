From 15f563661b8fc64d47e46b40c8ae7de46e201504 Mon Sep 17 00:00:00 2001
From: kake <kake@build.local>
Date: Tue, 25 Aug 2026 10:44:02 +0000
Subject: [PATCH 28/34] SystemUI: Full-screen landscape Material You QS and
 dynamic Keyguard QS solid black backdrop

Change-Id: I9c813a3fbb1961565d3d1e1ac6bbdc64cc3b4ba7
---
 packages/SystemUI/res/values-land/dimens.xml  |  2 +-
 .../res/values-w550dp-land/dimens.xml         |  2 +-
 .../com/android/systemui/qs/QuickQSPanel.java |  2 +-
 .../phone/NotificationPanelView.java          |  5 +++
 .../statusbar/phone/ScrimController.java      | 38 ++++++++++++++-----
 5 files changed, 36 insertions(+), 13 deletions(-)

diff --git a/packages/SystemUI/res/values-land/dimens.xml b/packages/SystemUI/res/values-land/dimens.xml
index 08087ebc..a3d1275b 100644
--- a/packages/SystemUI/res/values-land/dimens.xml
+++ b/packages/SystemUI/res/values-land/dimens.xml
@@ -30,7 +30,7 @@
     <dimen name="battery_detail_graph_space_top">9dp</dimen>
     <dimen name="battery_detail_graph_space_bottom">9dp</dimen>
 
-    <integer name="quick_settings_num_columns">2</integer>
+    <integer name="quick_settings_num_columns">4</integer>
     <bool name="quick_settings_wide">true</bool>
     <dimen name="qs_detail_margin_top">0dp</dimen>
 
diff --git a/packages/SystemUI/res/values-w550dp-land/dimens.xml b/packages/SystemUI/res/values-w550dp-land/dimens.xml
index 017ca698..88271465 100644
--- a/packages/SystemUI/res/values-w550dp-land/dimens.xml
+++ b/packages/SystemUI/res/values-w550dp-land/dimens.xml
@@ -17,6 +17,6 @@
 -->
 <resources>
     <!-- Standard notification width + gravity -->
-    <dimen name="notification_panel_width">544dp</dimen>
+    <dimen name="notification_panel_width">@dimen/match_parent</dimen>
 
 </resources>
diff --git a/packages/SystemUI/src/com/android/systemui/qs/QuickQSPanel.java b/packages/SystemUI/src/com/android/systemui/qs/QuickQSPanel.java
index 9ce4ee90..30bc8b83 100644
--- a/packages/SystemUI/src/com/android/systemui/qs/QuickQSPanel.java
+++ b/packages/SystemUI/src/com/android/systemui/qs/QuickQSPanel.java
@@ -204,7 +204,7 @@ public class QuickQSPanel extends QSPanel {
         }
 
         private int getNumColumns() {
-            return 2;
+            return getResources().getConfiguration().orientation == Configuration.ORIENTATION_LANDSCAPE ? 4 : 2;
         }
 
         private int getNumRows() {
diff --git a/packages/SystemUI/src/com/android/systemui/statusbar/phone/NotificationPanelView.java b/packages/SystemUI/src/com/android/systemui/statusbar/phone/NotificationPanelView.java
index 88dc95cf..31751b78 100644
--- a/packages/SystemUI/src/com/android/systemui/statusbar/phone/NotificationPanelView.java
+++ b/packages/SystemUI/src/com/android/systemui/statusbar/phone/NotificationPanelView.java
@@ -223,6 +223,7 @@ public class NotificationPanelView extends PanelView implements
     private final NotificationWakeUpCoordinator mWakeUpCoordinator;
     private final PulseExpansionHandler mPulseExpansionHandler;
     private final KeyguardBypassController mKeyguardBypassController;
+    private ScrimController mScrimController;
     @VisibleForTesting
     protected KeyguardUpdateMonitor mUpdateMonitor;
     @VisibleForTesting
@@ -1899,6 +1900,9 @@ public class NotificationPanelView extends PanelView implements
         float qsExpansionFraction = getQsExpansionFraction();
         mQs.setQsExpansion(qsExpansionFraction, getHeaderTranslation());
         mNotificationStackScroller.setQsExpansionFraction(qsExpansionFraction);
+        if (mScrimController != null) {
+            mScrimController.setQsExpansionFraction(qsExpansionFraction);
+        }
     }
 
     private String determineAccessibilityPaneTitle() {
@@ -3525,6 +3529,7 @@ public class NotificationPanelView extends PanelView implements
         mNotificationStackScroller.setGroupManager(groupManager);
         mNotificationStackScroller.setShelf(notificationShelf);
         mNotificationStackScroller.setScrimController(scrimController);
+        mScrimController = scrimController;
         updateShowEmptyShadeView();
     }
 
diff --git a/packages/SystemUI/src/com/android/systemui/statusbar/phone/ScrimController.java b/packages/SystemUI/src/com/android/systemui/statusbar/phone/ScrimController.java
index 2a321400..236c3d79 100644
--- a/packages/SystemUI/src/com/android/systemui/statusbar/phone/ScrimController.java
+++ b/packages/SystemUI/src/com/android/systemui/statusbar/phone/ScrimController.java
@@ -445,6 +445,20 @@ public class ScrimController implements ViewTreeObserver.OnPreDrawListener, OnCo
         }
     }
 
+    private float mQsExpansionFraction = 0.0f;
+
+    public void setQsExpansionFraction(float fraction) {
+        if (mQsExpansionFraction != fraction) {
+            mQsExpansionFraction = fraction;
+            if (mState == ScrimState.KEYGUARD) {
+                applyExpansionToAlpha();
+                if (!mUpdatePending) {
+                    setOrAdaptCurrentAnimation(mScrimBehind);
+                }
+            }
+        }
+    }
+
     private void applyExpansionToAlpha() {
         if (!mExpansionAffectsAlpha) {
             return;
@@ -458,21 +472,25 @@ public class ScrimController implements ViewTreeObserver.OnPreDrawListener, OnCo
             mCurrentBehindTint = Color.BLACK;
             mCurrentInFrontAlpha = 0;
         } else if (mState == ScrimState.KEYGUARD || mState == ScrimState.PULSING) {
-            // Either darken of make the scrim transparent when you
-            // pull down the shade
             float interpolatedFract = getInterpolatedFraction();
             float alphaBehind = mState.getBehindAlpha();
-            if (mDarkenWhileDragging) {
-                mCurrentBehindAlpha = MathUtils.lerp(GRADIENT_SCRIM_ALPHA_BUSY, alphaBehind,
-                        interpolatedFract);
+            if (mQsExpansionFraction > 0.0f) {
+                mCurrentBehindAlpha = MathUtils.lerp(alphaBehind, 1.0f, mQsExpansionFraction);
+                mCurrentBehindTint = Color.BLACK;
                 mCurrentInFrontAlpha = mState.getFrontAlpha();
             } else {
-                mCurrentBehindAlpha = MathUtils.lerp(0 /* start */, alphaBehind,
-                        interpolatedFract);
-                mCurrentInFrontAlpha = mState.getFrontAlpha();
+                if (mDarkenWhileDragging) {
+                    mCurrentBehindAlpha = MathUtils.lerp(GRADIENT_SCRIM_ALPHA_BUSY, alphaBehind,
+                            interpolatedFract);
+                    mCurrentInFrontAlpha = mState.getFrontAlpha();
+                } else {
+                    mCurrentBehindAlpha = MathUtils.lerp(0 /* start */, alphaBehind,
+                            interpolatedFract);
+                    mCurrentInFrontAlpha = mState.getFrontAlpha();
+                }
+                mCurrentBehindTint = ColorUtils.blendARGB(ScrimState.BOUNCER.getBehindTint(),
+                        mState.getBehindTint(), interpolatedFract);
             }
-            mCurrentBehindTint = ColorUtils.blendARGB(ScrimState.BOUNCER.getBehindTint(),
-                    mState.getBehindTint(), interpolatedFract);
         }
         if (isNaN(mCurrentBehindAlpha) || isNaN(mCurrentInFrontAlpha)) {
             throw new IllegalStateException("Scrim opacity is NaN for state: " + mState
-- 
2.43.0

