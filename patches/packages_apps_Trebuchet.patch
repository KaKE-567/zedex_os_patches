diff --git a/quickstep/src/com/android/launcher3/QuickstepAppTransitionManagerImpl.java b/quickstep/src/com/android/launcher3/QuickstepAppTransitionManagerImpl.java
index a91410c..44a5b20 100644
--- a/quickstep/src/com/android/launcher3/QuickstepAppTransitionManagerImpl.java
+++ b/quickstep/src/com/android/launcher3/QuickstepAppTransitionManagerImpl.java
@@ -107,14 +107,14 @@ public abstract class QuickstepAppTransitionManagerImpl extends LauncherAppTrans
     private static final String CONTROL_REMOTE_APP_TRANSITION_PERMISSION =
             "android.permission.CONTROL_REMOTE_APP_TRANSITION_ANIMATIONS";
 
-    private static final long APP_LAUNCH_DURATION = 450;
+    private static final long APP_LAUNCH_DURATION = 260;
     // Use a shorter duration for x or y translation to create a curve effect
-    private static final long APP_LAUNCH_CURVED_DURATION = 250;
-    private static final long APP_LAUNCH_ALPHA_DURATION = 50;
-    private static final long APP_LAUNCH_ALPHA_START_DELAY = 25;
+    private static final long APP_LAUNCH_CURVED_DURATION = 175;
+    private static final long APP_LAUNCH_ALPHA_DURATION = 35;
+    private static final long APP_LAUNCH_ALPHA_START_DELAY = 0;
 
     // We scale the durations for the downward app launch animations (minus the scale animation).
-    private static final float APP_LAUNCH_DOWN_DUR_SCALE_FACTOR = 0.8f;
+    private static final float APP_LAUNCH_DOWN_DUR_SCALE_FACTOR = 0.85f;
     private static final long APP_LAUNCH_DOWN_DURATION =
             (long) (APP_LAUNCH_DURATION * APP_LAUNCH_DOWN_DUR_SCALE_FACTOR);
     private static final long APP_LAUNCH_DOWN_CURVED_DURATION =
@@ -122,15 +122,15 @@ public abstract class QuickstepAppTransitionManagerImpl extends LauncherAppTrans
     private static final long APP_LAUNCH_ALPHA_DOWN_DURATION =
             (long) (APP_LAUNCH_ALPHA_DURATION * APP_LAUNCH_DOWN_DUR_SCALE_FACTOR);
 
-    private static final long CROP_DURATION = 375;
-    private static final long RADIUS_DURATION = 375;
+    private static final long CROP_DURATION = 240;
+    private static final long RADIUS_DURATION = 240;
 
-    public static final int RECENTS_LAUNCH_DURATION = 336;
-    private static final int LAUNCHER_RESUME_START_DELAY = 100;
-    private static final int CLOSING_TRANSITION_DURATION_MS = 250;
+    public static final int RECENTS_LAUNCH_DURATION = 240;
+    private static final int LAUNCHER_RESUME_START_DELAY = 0;
+    private static final int CLOSING_TRANSITION_DURATION_MS = 220;
 
-    protected static final int CONTENT_ALPHA_DURATION = 217;
-    protected static final int CONTENT_TRANSLATION_DURATION = 350;
+    protected static final int CONTENT_ALPHA_DURATION = 140;
+    protected static final int CONTENT_TRANSLATION_DURATION = 220;
 
     // Progress = 0: All apps is fully pulled up, Progress = 1: All apps is fully pulled down.
     public static final float ALL_APPS_PROGRESS_OFF_SCREEN = 1.3059858f;
@@ -513,13 +513,13 @@ public abstract class QuickstepAppTransitionManagerImpl extends LauncherAppTrans
             FloatProp mDx = new FloatProp(0, dX, 0, xDuration, AGGRESSIVE_EASE);
             FloatProp mDy = new FloatProp(0, dY, 0, yDuration, AGGRESSIVE_EASE);
             FloatProp mIconScale = new FloatProp(initialStartScale, scale, 0, APP_LAUNCH_DURATION,
-                    EXAGGERATED_EASE);
+                    AGGRESSIVE_EASE);
             FloatProp mIconAlpha = new FloatProp(1f, 0f, APP_LAUNCH_ALPHA_START_DELAY,
                     alphaDuration, LINEAR);
             FloatProp mCroppedSize = new FloatProp(startCrop, endCrop, 0, CROP_DURATION,
-                    EXAGGERATED_EASE);
+                    AGGRESSIVE_EASE);
             FloatProp mWindowRadius = new FloatProp(initialWindowRadius, windowRadius, 0,
-                    RADIUS_DURATION, EXAGGERATED_EASE);
+                    RADIUS_DURATION, AGGRESSIVE_EASE);
 
             @Override
             public void onUpdate(float percent) {
@@ -728,8 +728,8 @@ public abstract class QuickstepAppTransitionManagerImpl extends LauncherAppTrans
                     mDragLayerAlpha, MultiValueAlpha.VALUE, 0, 1f));
 
             workspaceAnimator.setStartDelay(LAUNCHER_RESUME_START_DELAY);
-            workspaceAnimator.setDuration(333);
-            workspaceAnimator.setInterpolator(Interpolators.DEACCEL_1_7);
+            workspaceAnimator.setDuration(220);
+            workspaceAnimator.setInterpolator(Interpolators.AGGRESSIVE_EASE);
 
             mDragLayer.getScrim().hideSysUiScrim(true);
 
diff --git a/res/values/lineage_strings.xml b/res/values/lineage_strings.xml
index 40200b4..4fcdf55 100644
--- a/res/values/lineage_strings.xml
+++ b/res/values/lineage_strings.xml
@@ -60,4 +60,8 @@
     <string name="icon_pack_default_label">Default</string>
     <string name="icon_pack_add">Install more</string>
     <string name="icon_pack_no_market">There\'s no available app store</string>
+
+    <!-- Icon size -->
+    <string name="icon_size_title">Icon size</string>
+    <string name="icon_size_summary">Adjust the size of app icons</string>
 </resources>
diff --git a/res/xml/launcher_preferences.xml b/res/xml/launcher_preferences.xml
index 87ffb46..905612f 100644
--- a/res/xml/launcher_preferences.xml
+++ b/res/xml/launcher_preferences.xml
@@ -15,7 +15,8 @@
 -->
 
 <androidx.preference.PreferenceScreen
-    xmlns:android="http://schemas.android.com/apk/res/android">
+    xmlns:android="http://schemas.android.com/apk/res/android"
+    xmlns:app="http://schemas.android.com/apk/res-auto">
 
     <PreferenceCategory
         android:key="category_desktop"
@@ -47,6 +48,17 @@
         android:key="category_apps"
         android:title="@string/settings_category_apps">
 
+        <androidx.preference.SeekBarPreference
+            android:key="pref_icon_size"
+            android:title="@string/icon_size_title"
+            android:summary="@string/icon_size_summary"
+            android:defaultValue="100"
+            android:max="140"
+            app:min="70"
+            android:persistent="true"
+            app:showSeekBarValue="true"
+            app:updatesContinuously="false" />
+
         <Preference
             android:key="pref_trust_apps"
             android:title="@string/trust_apps_manager_name" />
diff --git a/src/com/android/launcher3/InvariantDeviceProfile.java b/src/com/android/launcher3/InvariantDeviceProfile.java
index f67d868..62367f2 100644
--- a/src/com/android/launcher3/InvariantDeviceProfile.java
+++ b/src/com/android/launcher3/InvariantDeviceProfile.java
@@ -81,6 +81,7 @@ public class InvariantDeviceProfile implements OnSharedPreferenceChangeListener
     public static final String KEY_SHOW_LABELS_LANDSCAPE = "pref_show_labels_landscape";
     public static final String KEY_ICON_PATH_REF = "pref_icon_shape_path";
     public static final String KEY_WORKSPACE_EDIT = "pref_workspace_edit";
+    public static final String KEY_ICON_SIZE = "pref_icon_size";
 
     // Constants that affects the interpolation curve between statically defined device profile
     // buckets.
@@ -192,7 +193,7 @@ public class InvariantDeviceProfile implements OnSharedPreferenceChangeListener
     public void onSharedPreferenceChanged(SharedPreferences prefs, String key) {
         if (KEY_SHOW_DESKTOP_LABELS.equals(key) || KEY_SHOW_DRAWER_LABELS.equals(key)) {
             apply(mContext, CHANGE_FLAG_ICON_PARAMS);
-        } else if (KEY_SHOW_LABELS_LANDSCAPE.equals(key)) {
+        } else if (KEY_SHOW_LABELS_LANDSCAPE.equals(key) || KEY_ICON_SIZE.equals(key)) {
             onConfigChanged(mContext);
         }
     }
@@ -315,10 +316,11 @@ public class InvariantDeviceProfile implements OnSharedPreferenceChangeListener
 
         mExtraAttrs = closestProfile.extraAttrs;
 
-        iconSize = displayOption.iconSize;
+        float iconScale = Utilities.getPrefs(context).getInt(KEY_ICON_SIZE, 100) / 100.0f;
+        iconSize = displayOption.iconSize * iconScale;
         iconShapePath = getIconShapePath(context);
         iconPack = new IconPackStore(context).getCurrent();
-        landscapeIconSize = displayOption.landscapeIconSize;
+        landscapeIconSize = displayOption.landscapeIconSize * iconScale;
         iconBitmapSize = ResourceUtils.pxFromDp(iconSize, metrics);
         iconTextSize = displayOption.iconTextSize;
         fillResIconDpi = getLauncherIconDensity(iconBitmapSize);
